name: animation_director
description: A package to create nice and smooth animations for flutter
version: 0.0.1
homepage: https://github.com/Ali-Azmoud/animation_director

environment:
  sdk: ">=2.3.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
